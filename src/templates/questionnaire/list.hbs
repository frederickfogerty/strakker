<div class="row">
    <div class="small-12 columns">
        <table>
            <thead>
                <tr>
                    <th width="10%">ID</th>
                    <th width="30%">Name</th>
                    <th width="30%">Date Created</th>
                    <th width="30%">Swag</th>
                </tr>
            </thead>
            <tbody>
                {{#each questionnaire in model}}
                    {{#with questionnaire}}
                        <tr>
                            <td>{{id}}</td>
                            <td>{{name}}</td>
                            <td>{{created}}</td>
                            <td>Yes</td>
                        </tr>
                    {{/with}}
                {{/each}}</tbody>
        </table>
    </div>
</div>